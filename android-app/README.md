# Cardiac Monitor — Android App

Companion Android app for the STM32 cardiac monitor. Displays live readings from the device.

Install: side-load `app-debug cardiac monitor.apk` on an Android device (this is a debug build, so you may need to enable "install from unknown sources").
